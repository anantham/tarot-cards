# Web3.Storage Credentials
# Generate these using the commands in README.md (Task 11)

# Step 1: Run `cat server_identity.key` and copy the output here
WEB3_STORAGE_AGENT_KEY=Ed25519PrivateKey:base58:YOUR_PRIVATE_KEY_HERE

# Step 2: Run `cat delegation.car | base64` and copy the output here
WEB3_STORAGE_DELEGATION_PROOF=YOUR_BASE64_ENCODED_DELEGATION_HERE

# Gemini API Key (for video generation proxy)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Vercel KV (auto-populated in production by Vercel)
# For local development, get these from Vercel dashboard → Storage → KV → .env.local
KV_REST_API_URL=https://YOUR_KV_INSTANCE.kv.vercel-storage.com
KV_REST_API_TOKEN=YOUR_KV_TOKEN_HERE
